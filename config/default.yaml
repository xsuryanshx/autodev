repo:
  owner: ""
  name: ""
  branch: "main"

issue:
  number: null

agents:
  max_parallel: 4
  timeout_per_subtask: 600  # seconds

research:
  trigger_after_failures: 3
  max_research_time: 300

github:
  auto_create_pr: true
  auto_merge: false
  add_reviewers: []

testing:
  run_before_pr: true
  coverage_threshold: 80

skills:
  - code_review
  - write_tests
  - run_tests
  - fix_errors
